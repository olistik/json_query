require 'spec_helper'

describe JsonQuery do
  it 'has a version number' do
    expect(JsonQuery::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
